---
- name: Converge
  hosts: all
  become: yes
  tasks:
    - name: "Include vmalert"
      vars:
        vmalert_enterprise: true
        vmalert_license_key: "{{ lookup('env', 'VM_LICENSE_KEY') }}"
        vmalert_license_key_file: "{{ '' if lookup('env', 'VM_LICENSE_KEY') else '/tmp/vm-license' }}"
      ansible.builtin.include_role:
        name: "vmalert"
