---
- name: Converge
  hosts: all
  become: yes
  tasks:
    - name: "Include vmagent"
      vars:
        vmagent_service_args:
          "remoteWrite.url":
            - "http://url1/api/v1/write"
            - "http://url2/api/v1/write"
          "remoteWrite.tmpDataPath": /tmp/vmagent
        vmagent_service_envflag_enabled: true
        vmagent_service_envflag_data:
          - "http_pathPrefix=/vmagent"
      ansible.builtin.include_role:
        name: "vmagent"
