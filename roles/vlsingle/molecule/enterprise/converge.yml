---
- name: Converge
  hosts: all
  become: yes
  vars:
    vlsingle_enterprise: true
    vlsingle_license_key: "{{ lookup('env', 'VM_LICENSE_KEY') }}"
    vlsingle_license_key_file: "{{ '' if lookup('env', 'VM_LICENSE_KEY') else '/tmp/vl-license' }}"
    vlsingle_data_dir: "/tmp/victoria-logs/"
    vlsingle_service_envflag_enabled: true
    vlsingle_service_envflag_data:
      - "syslog_listenAddr_tcp=127.0.0.1:12345"
    vlsingle_service_args:
      storageDataPath: "{{ vlsingle_data_dir }}"
  tasks:
    - name: "Include vlsingle"
      ansible.builtin.include_role:
        name: "vlsingle"
