# handlers file for VictoriaLogs
---
- name: Restart VictoriaLogs service
  become: true
  ansible.builtin.systemd:
    name: "{{ vlsingle_service_name }}"
    state: restarted
  when: not ansible_check_mode
